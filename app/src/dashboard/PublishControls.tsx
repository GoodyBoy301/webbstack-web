import type { Visibility } from "../projectPublishing";
type Props = { visibility: Visibility; setVisibility: (value: Visibility) => void; onPublish: () => void; onCopy: () => void; hasPublishedUrl: boolean; onOpenPublic: () => void };
export function PublishControls({ visibility, setVisibility, onPublish, onCopy, hasPublishedUrl, onOpenPublic }: Props) { return <><label className="visibility-field">Visibility<select name="visibility" value={visibility} onChange={(event) => setVisibility(event.target.value as Visibility)}><option value="public">Public — discoverable by anyone with the link</option><option value="unlisted">Unlisted — only people with the link</option><option value="private">Private — keep in your workspace</option></select></label><div className="publish-actions"><button className="dashboard-primary" onClick={onPublish}>{hasPublishedUrl ? "Update visibility" : "Publish showcase"}</button><button className="project-action" onClick={onCopy}>Copy URL</button>{hasPublishedUrl && <button className="project-action" onClick={onOpenPublic}>Open public viewer</button>}</div></>; }
